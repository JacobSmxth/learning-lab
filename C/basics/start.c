#include <math.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

  // FIrst ever program!

  /*
    16:00:37
  */

  // printf("I like steak!\n");

  // printf("So yummy!\n");

  // int age = 25;
  // int year = 2025;
  // int quantity = 10;
  // float pi = 3.14;

  // printf("The year is %d\n", year);
  // printf("You have ordered %d x items\n", quantity);
  // printf("You are %d years old\n", age);
  // printf("The value of pi is %.2f\n", pi);

  // double pi = 3.14159265358979;

  // printf("Pi is %lf\n", pi);

  // char grade = 'A';
  // char symbol = '$';

  // printf("The symbol is a %c\n", symbol);
  // printf("Your grade is %c\n", grade);

  // char food[] = "Steak";
  // char name[] = "Jacob Smith";
  // char email[] = "jacob.d.smith@live.com";
  // printf("Hello %s and I eat %s\nMy email is %s\n", name, food, email);

  // bool isOnline = true;
  // bool isStudent = true;

  // if (isOnline && isStudent) {
  //   printf("He is online!\n");
  // } else {
  //   printf("He ran away. Die \n");
  // }

  // int age = 25;
  // float price = 19.99;
  // char dollar = '$';
  // char name[] = "Jacob Smith";

  // printf("%d\n", age);
  // printf("%.1f\n", price);
  // printf("%c\n", dollar);
  // printf("%s\n", name);

  // float price1 = 199.99;
  // float price2 = 21.99;
  // float price3 = 4.99;
  // char dollar = '$';

  // printf("%c%.2f\n", dollar, price2);

  // int x = 5;
  // int y = 3;
  // int z = 0;

  // z = x + y;
  // z = x - y;
  // z = x * y;
  // z = x / y;
  // z = x % y;

  // printf("%d\n", z);

  // int age = 0;
  // float gpa = 0.0f;
  // char grade = '\0';
  // char name[30] = "";

  // printf("Enter your age: ");
  // scanf("%d", &age);

  // printf("Enter your GPA: ");
  // scanf("%f", &gpa);

  // printf("Enter your grade: ");
  // scanf(" %c", &grade);

  // getchar();
  // printf("Enter your full name: ");
  // fgets(name, sizeof(name), stdin);
  // name[strlen(name) - 1] = '\0';

  // printf("%s\n", name);
  // printf("%d\n", age);
  // printf("%.2f\n", gpa);
  // printf("%c\n", grade);

  // char item[30] = "";
  // float price = 0.0f;
  // char currency = '$';
  // int quantity = 0;
  // float total = 0.0f;

  // printf("What item would you like to buy?: ");
  // fgets(item, sizeof(item), stdin);
  // item[strlen(item) - 1] = '\0';

  // printf("What is the price for the item?: ");
  // scanf("%f", &price);

  // printf("How many would you like?: ");
  // scanf("%d", &quantity);

  // total = price * quantity;

  // printf("\nYou have bought %d %s/s\n\n", quantity, item);
  // printf("The total is: %c%.2f\n", currency, total);

  // int x = -3;

  // x = sqrt(x);
  // x = pow(x, 4);
  // x = round(x);
  // x = ceil(x);
  // x = floor(x);
  // x = log(x);
  // x = abs(x);

  // printf("%d\n", x);

  return 0;
}
